// AppShell is a layout-route component: it takes no props of its own and
// renders its matched child route via `<Outlet/>`. Kept as an explicit
// (empty) type for the 4-file component convention.
export type AppShellProps = Record<string, never>
