// The banner derives everything from the wall clock, so it takes no props.
// Kept as an explicit (empty) type for the 4-file component convention.
export type MarketStatusBannerProps = Record<string, never>
